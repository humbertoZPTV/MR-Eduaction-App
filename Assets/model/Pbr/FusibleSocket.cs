using UnityEngine;

public class FusibleSocket : MonoBehaviour
{
    //Este script, en principio, no necesita hacer nada más.
    //Toda la lógica se gestiona en el script del fusible y en el GameManager.

    //Si quieres poner un efecto visual al acertar/fallar, pon aqui una referencia a un sistema de particulas y haz un
    //.Play() cuando corresponda.
}